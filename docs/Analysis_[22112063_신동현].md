# 2. Analysis

## KTX-AutoReserve

![ktx_autoreserve](https://donghyeun-blog-images.s3.us-east-1.amazonaws.com/yu_report_ktx_autoreserve.png)

(Student No, Name, E-mail)
22112063, 신동현, sindong106@yu.ac.kr

GitHub Repository : https://github.com/donghyeun02/KorailAutoReserve

---

## Revision history

| Revision date | Version # | Description | Author |
|---|---|---|---|
| 2026.03.27 | 1.0.0 | First Draft | 신동현 |
| 2026.06.11 | 1.1.0 | Java(Spring Boot/Selenium) → Python(FastAPI/selenium-driverless) 스택 이관 | 신동현 |

---

## Contents

1. Introduction
2. Use case analysis
3. Domain analysis
4. User Interface prototype
5. Glossary
6. References

---

## 1. Introduction

이 문서는 'KTX-AutoReserve'라는 코레일 KTX 승차권 자동 예매 시스템에 대한 분석(Analysis) 보고서입니다.

KTX-AutoReserve는 사용자가 단일 API 요청만으로 코레일 웹사이트에 로그인하고, 원하는 조건의 KTX 열차를 자동으로 탐색하여 예약을 시도하는 FastAPI 기반의 웹 자동화 시스템입니다. selenium-driverless를 통해 브라우저 UI를 제어하며, 로그인부터 예약 버튼 클릭까지의 전 과정을 일관된 로직으로 수행합니다.

이 보고서에서는 KTX-AutoReserve 프로젝트가 가진 다음 특징들을 중심으로 분석 내용을 담고 있습니다.

**usefulness**: 명절·연휴·출퇴근 시간대 등 경쟁이 치열한 KTX 좌석 예매 환경에서, 사용자가 반복적으로 화면을 새로고침하며 수동으로 클릭해야 하는 부담을 없애고 일관된 자동화 로직으로 예약 성공률과 신속성을 높이는 데 실질적인 도움을 제공합니다.

**significance**: selenium-driverless 기반의 브라우저 UI 자동화와 FastAPI REST API를 결합하여, 단일 HTTP 요청 하나만으로 로그인~예약 시도까지의 전 과정을 처리하는 차별화된 구조를 갖습니다. 또한 AWS SNS 연동을 통해 예약 성공 즉시 SMS 알림을 전송하는 기능도 포함합니다.

**expandability**: 현재 KTX 예매에 집중되어 있으나, 출발역·도착역·날짜·시간 조건을 파라미터화한 구조 덕분에 다양한 노선·열차 유형으로 확장하거나, 복수 예약 시도·스케줄 기반 자동 실행 등으로 기능을 발전시킬 수 있는 잠재력을 가집니다.

이어서 보고서에서는 KTX-AutoReserve 시스템의 구체적인 요구사항 및 구조를 파악하기 위한 Use Case 분석, Domain 분석, 그리고 사용자 인터페이스 프로토타입 소개가 이어집니다.

---

## 2. Use case analysis

### 2.1 Use Case Diagram

아래는 KTX-AutoReserve 시스템의 Use Case Diagram을 나타낸 것이다.

![ktx_autoreserve_use_case](https://donghyeun-blog-images.s3.us-east-1.amazonaws.com/ktx_autoreserve_use_case.jpg)

위 Diagram에서 Actor는 **User**(API 호출자), **Korail Web**(외부 코레일 웹 시스템), **AWS SNS**(문자 발송 외부 서비스) 세 개로 구성된다. User는 **Reserve KTX Ticket** 하나를 직접 트리거하며, 파라미터 검증과 열차 탐색·예약은 include 관계로 항상 수행되고, 예외 처리와 SMS 발송은 조건에 따라 발동하는 extend 관계로 분리된다.

아래는 각 Use Case의 ID, 이름, Actor를 나타낸 표이다.

| Use Case Name | Use Case ID | Korean Name | Actor |
|---|---|---|---|
| Reserve KTX Ticket | #1 | KTX 티켓 예약 | User |
| Validate Parameters | #2 | 요청 파라미터 검증 | System (TicketController) |
| Search & Attempt Reservation | #3 | 열차 탐색 및 예약 시도 | System (TicketService), Korail Web |
| Handle Exception | #4 | 예외 처리 | System (GlobalExceptionHandler) |
| Send SMS Notification | #5 | SMS 알림 발송 | System (SmsSender), AWS SNS |

Use Case Description에서는 위에서부터 차례대로 각 Use Case에 대해 표로 Description을 보여줄 것이다.

---

### 2.2 Use Case Description

#### 2.2.1 Reserve KTX Ticket

| Use Case #1 : Reserve KTX Ticket | |
|---|---|
| **GENERAL CHARACTERISTICS** | |
| Summary | 사용자가 Swagger UI 또는 HTTP 클라이언트를 통해 출발역, 도착역, 날짜, 시간 범위 등 필수 파라미터를 입력하고 `/ticket/reserve` API를 호출하여 자동 예매 프로세스를 시작한다. |
| Scope / Level | KTX-AutoReserve System / User level |
| Author | 신동현 |
| Last Update | 2026.05.07 |
| Status | Analysis |
| Primary Actor | User |
| Secondary Actors | System (TicketController) |
| Preconditions | 1. 시스템이 실행 중이어야 한다. 2. 사용자는 유효한 코레일 계정 정보를 보유하고 있어야 한다. |
| Trigger | 사용자가 `/ticket/reserve` 엔드포인트로 HTTP GET 요청을 전송할 때 |
| Success Post Condition | 1. 파라미터 검증을 통과하고 자동화 프로세스가 완료된다. 2. 예약 성공 메시지가 응답으로 반환된다. |
| Failed Post Condition | 1. 파라미터 오류 시 400 응답이 반환된다. 2. 예약 실패 시 실패 메시지가 반환된다. |

| MAIN SUCCESS SCENARIO | |
|---|---|
| Step | Action |
| S | 사용자가 Swagger UI에서 예약 조건 파라미터를 입력하고 요청을 준비한다. |
| 1 | 이 Use case는 사용자가 `/ticket/reserve`로 HTTP GET 요청을 전송할 때 시작된다. |
| 2 | 시스템은 요청을 수신하고 파라미터를 검증한다 (Use Case #2 <<include>>). |
| 3 | 검증 통과 후 열차 탐색 및 예약을 수행한다 (Use Case #3 <<include>>). |
| 4 | 예약 성공 메시지를 HTTP 응답으로 반환한다. |

| EXTENSION SCENARIOS | |
|---|---|
| Step | Branching Action |
| 2 | 2a. 파라미터 검증 실패 시 → Use Case #4 (Handle Exception)가 400 응답을 반환한다. |
| 3 | 3a. 예약 성공 후 전화번호 파라미터가 존재하는 경우 → Use Case #5 (Send SMS Notification)가 발동된다. |

---

#### 2.2.2 Validate Parameters

| Use Case #2 : Validate Parameters | |
|---|---|
| **GENERAL CHARACTERISTICS** | |
| Summary | 시스템이 사용자 요청의 파라미터(출발역, 도착역, 날짜, 시간 범위 등)의 누락, 타입 오류, 시간 순서 오류를 검증한다. |
| Scope / Level | KTX-AutoReserve System / System level |
| Author | 신동현 |
| Last Update | 2026.05.07 |
| Status | Analysis |
| Primary Actor | System (TicketController) |
| Secondary Actors | - |
| Preconditions | 1. `/ticket/reserve` 요청이 수신된 상태여야 한다. |
| Trigger | Use Case #1에 의해 <<include>>로 포함될 때 |
| Success Post Condition | 1. 모든 파라미터가 유효하다고 판단된다. 2. Use Case #3 실행이 진행된다. |
| Failed Post Condition | 1. 유효하지 않은 파라미터가 감지된다. 2. Use Case #4가 발동되어 400 응답이 반환된다. |

| MAIN SUCCESS SCENARIO | |
|---|---|
| Step | Action |
| S | 시스템이 수신된 요청의 파라미터를 검증한다. |
| 1 | 이 Use case는 Use Case #1이 실행될 때 <<include>>로 시작된다. |
| 2 | 시스템은 필수 파라미터(출발역, 도착역, 날짜, 시간 시작, 시간 종료)의 존재 여부를 확인한다. |
| 3 | 시스템은 각 파라미터의 타입 및 형식을 검증한다. |
| 4 | 시스템은 시작 시간이 종료 시간보다 앞서는지 확인한다. |
| 5 | 검증을 통과하면 Use Case #3 실행을 진행한다. |

| EXTENSION SCENARIOS | |
|---|---|
| Step | Branching Action |
| 2 | 2a. 필수 파라미터 누락 시 → "필수 파라미터 누락" 메시지와 함께 Use Case #4가 400 응답을 반환한다. |
| 3 | 3a. 타입 오류 발생 시 → "파라미터 타입 오류" 메시지와 함께 Use Case #4가 400 응답을 반환한다. |
| 4 | 4a. 시간 순서가 잘못된 경우 → "시작 시간은 종료 시간보다 앞서야 합니다" 메시지와 함께 Use Case #4가 400 응답을 반환한다. |

---

#### 2.2.3 Search & Attempt Reservation

| Use Case #3 : Search & Attempt Reservation | |
|---|---|
| **GENERAL CHARACTERISTICS** | |
| Summary | 시스템이 브라우저를 실행하고 코레일에 로그인한 후, 조건에 맞는 KTX 열차를 반복 탐색하여 예약 버튼을 클릭하고 예약을 완료한다. |
| Scope / Level | KTX-AutoReserve System / System level |
| Author | 신동현 |
| Last Update | 2026.05.07 |
| Status | Analysis |
| Primary Actor | System (TicketService) |
| Secondary Actors | Korail Web |
| Preconditions | 1. Use Case #2 파라미터 검증이 완료된 상태여야 한다. |
| Trigger | Use Case #1에 의해 <<include>>로 포함될 때 |
| Success Post Condition | 1. 코레일 웹에서 KTX 예약이 완료된다. 2. 예약 성공 메시지가 반환된다. |
| Failed Post Condition | 1. 설정된 재시도 횟수 내에 조건에 맞는 열차를 찾지 못한 경우 실패 메시지를 반환한다. 2. WebDriver 세션이 정리된다. |

| MAIN SUCCESS SCENARIO | |
|---|---|
| Step | Action |
| S | 시스템이 브라우저 자동화를 통해 코레일 예약을 수행한다. |
| 1 | 이 Use case는 Use Case #1이 실행될 때 <<include>>로 시작된다. |
| 2 | WebDriverFactory가 Chrome 브라우저를 실행하고 WebDriver를 초기화한다. |
| 3 | KorailLoginService가 코레일 로그인 페이지에 접근하여 공지 팝업을 처리하고 로그인을 수행한다. |
| 4 | 시스템이 일반 승차권 예매 페이지로 이동한다. |
| 5 | SearchCriteriaService가 출발역, 도착역, 날짜, 시간 범위를 UI 팝업에서 입력하고 조회 버튼을 클릭한다. |
| 6 | TicketReservationService가 열차 목록을 순회하며 Time Window와 KTX Filter 조건을 검사한다. |
| 7 | 조건에 맞는 열차의 예약 버튼을 클릭한다. |
| 8 | 예약 완료를 확인하고 WebDriver 세션을 종료한다. |

| EXTENSION SCENARIOS | |
|---|---|
| Step | Branching Action |
| 3 | 3a. 팝업 오버레이가 클릭을 가로막는 경우 → Explicit Wait 적용 후 재시도한다. |
| 3 | 3b. 로그인 실패 시 → 오류 로깅 후 프로세스를 중단한다. |
| 6 | 6a. 조건에 맞는 열차가 없는 경우 → Polling 대기 후 재조회한다. |
| 6 | 6b. 최대 재시도 횟수 초과 시 → "조건에 맞는 열차를 찾지 못했습니다" 응답을 반환한다. |
| 7 | 7a. 예약 버튼이 비활성화(좌석 없음)인 경우 → Step 6 재탐색으로 돌아간다. |
| 7 | 7b. 팝업 오버레이가 버튼 클릭을 가로막는 경우 → 오버레이 제거 후 재시도한다. |

---

#### 2.2.4 Handle Exception

| Use Case #4 : Handle Exception | |
|---|---|
| **GENERAL CHARACTERISTICS** | |
| Summary | 시스템이 요청 처리 중 발생하는 파라미터 누락, 타입 오류 등의 예외를 감지하여 사용자 친화적인 400 응답 메시지로 변환한다. |
| Scope / Level | KTX-AutoReserve System / System level |
| Author | 신동현 |
| Last Update | 2026.05.07 |
| Status | Analysis |
| Primary Actor | System (GlobalExceptionHandler) |
| Secondary Actors | User |
| Preconditions | 1. 요청 처리 중 바인딩 예외 또는 파라미터 예외가 발생한 상태여야 한다. |
| Trigger | Use Case #1 처리 중 예외가 발생하여 <<extend>>로 발동될 때 |
| Success Post Condition | 1. 의미 있는 오류 메시지가 포함된 400 Bad Request 응답이 반환된다. |
| Failed Post Condition | - |

| MAIN SUCCESS SCENARIO | |
|---|---|
| Step | Action |
| S | 시스템이 발생한 예외를 처리한다. |
| 1 | 이 Use case는 Use Case #1 처리 중 예외가 발생할 때 <<extend>>로 자동 시작된다. |
| 2 | GlobalExceptionHandler가 발생한 예외 유형을 감지한다. |
| 3 | 예외 유형에 맞는 사용자 친화적 오류 메시지를 생성한다. |
| 4 | 400 Bad Request 상태 코드와 함께 오류 메시지를 응답으로 반환한다. |

| EXTENSION SCENARIOS | |
|---|---|
| Step | Branching Action |
| 2 | 2a. 예상하지 못한 예외 유형 발생 시 → 일반 오류 메시지와 함께 500 응답을 반환한다. |

---

#### 2.2.5 Send SMS Notification

| Use Case #5 : Send SMS Notification | |
|---|---|
| **GENERAL CHARACTERISTICS** | |
| Summary | 예약 성공 시 사용자가 파라미터로 입력한 전화번호로 AWS SNS를 통해 예약 완료 SMS를 발송한다. |
| Scope / Level | KTX-AutoReserve System / System level |
| Author | 신동현 |
| Last Update | 2026.05.07 |
| Status | Analysis |
| Primary Actor | System (SmsSender) |
| Secondary Actors | AWS SNS |
| Preconditions | 1. Use Case #3이 성공적으로 완료된 상태여야 한다. 2. 사용자가 전화번호 파라미터를 입력한 상태여야 한다. |
| Trigger | Use Case #1 처리 중 예약 성공 및 전화번호 파라미터 존재 조건이 충족되어 <<extend>>로 발동될 때 |
| Success Post Condition | 1. AWS SNS를 통해 예약 완료 SMS가 발송된다. |
| Failed Post Condition | 1. AWS SNS 호출 실패 시 오류를 로깅하고 API 응답은 정상적으로 반환된다. |

| MAIN SUCCESS SCENARIO | |
|---|---|
| Step | Action |
| S | 시스템이 예약 성공 후 SMS 알림을 발송한다. |
| 1 | 이 Use case는 예약 성공 및 전화번호 파라미터 존재 시 <<extend>>로 시작된다. |
| 2 | SmsSender가 AWS SNS 클라이언트를 통해 예약 완료 메시지를 구성한다. |
| 3 | AWS SNS가 해당 전화번호로 SMS를 발송한다. |

| EXTENSION SCENARIOS | |
|---|---|
| Step | Branching Action |
| 3 | 3a. AWS SNS 호출 실패 시 → 오류를 로깅하고 API 응답은 정상적으로 반환한다. |

---

## 3. Domain analysis

아래는 KTX-AutoReserve 시스템의 Domain Analysis에서 나오는 Class들의 관계를 나타낸 것이다.

```
┌─────────────────────────────────────────────────────────┐
│                   KorailReserveApplication              │
└──────────────────────────┬──────────────────────────────┘
                           │ starts
          ┌────────────────┼────────────────┐
          ▼                ▼                ▼
   TicketController  GlobalException   SwaggerConfig
          │            Handler
          │ calls
          ▼
    TicketService
     ┌────┼──────────────────────┐
     │    │                      │
     ▼    ▼                      ▼
WebDriver  KorailLogin      SearchCriteria
Factory    Service          Service
                                 │
                    ┌────────────┤
                    ▼            ▼
            TicketReservation  SmsSender
                Service
```

### 애플리케이션 진입점

**KorailReserveApplication**: FastAPI 애플리케이션의 진입점 클래스이다. `main()` 메서드에서 `SpringApplication.run()`을 호출하여 서버를 기동하고, 컨트롤러·서비스·설정 빈을 로드하여 API를 제공한다.

### 웹 계층

**TicketController**: HTTP 요청을 수신하는 REST 컨트롤러이다. `/ticket/reserve` 엔드포인트를 통해 사용자 요청을 받고, 쿼리 파라미터의 형식·범위·시간 순서를 검증한 후 TicketService를 호출한다. 잘못된 요청을 조기에 차단하여 정상 입력만 서비스 계층으로 전달한다.

**GlobalExceptionHandler**: `@RestControllerAdvice`로 구현된 전역 예외 처리 클래스이다. 누락 파라미터(`MissingServletRequestParameterException`)와 타입 오류(`MethodArgumentTypeMismatchException`) 예외를 처리하여 Whitelabel 오류 대신 의미 있는 400 응답을 제공한다.

**SwaggerConfig**: OpenAPI 문서 메타데이터를 설정하는 클래스이다. OpenAPI 빈을 등록하여 Swagger UI에서 API 테스트와 문서 확인이 가능하도록 한다. 앱 시작 시 초기화된다.

### 자동화 오케스트레이션

**TicketService**: 예약 자동화 전체 시나리오를 오케스트레이션하는 핵심 서비스 클래스이다. WebDriverFactory → KorailLoginService → 화면이동 → SearchCriteriaService → TicketReservationService 순서로 자동화 흐름을 제어한다. 요청 단위로 실행되며 종료 시 드라이버를 정리한다.

### 브라우저 제어

**WebDriverFactory**: 브라우저 드라이버 생성을 전담하는 클래스이다. Chrome/Safari 선택, 헤드리스 옵션, 창 크기 초기화 등을 담당하여 브라우저 실행 설정 변경 시 영향 범위를 최소화한다.

**KorailLoginService**: 코레일 로그인 UI 흐름을 처리하는 전용 클래스이다. 로그인 페이지 진입, 공지 팝업 닫기, 아이디/비밀번호 입력, 로그인 버튼 클릭을 순서대로 수행하며, 예약 절차 초반 1회 실행된다.

**SearchCriteriaService**: 조회 조건 입력 로직을 담당하는 클래스이다. 출발역/도착역 팝업 선택, 날짜 팝업 선택, 시간 슬라이더 조작을 수행하며, 재사용성과 테스트 용이성을 위해 TicketService로부터 분리되었다.

**TicketReservationService**: 열차 목록 판별 및 예약 클릭을 담당하는 클래스이다. 조회 결과 목록을 순회하며 출발 시각(Time Window)과 KTX 여부(KTX Filter)를 필터링하고, 조건에 맞는 열차의 예약 버튼을 클릭한다. 조회 루프 내에서 반복 실행된다.

### 알림

**SmsSender**: 예약 성공 시 SMS 알림을 전송하는 클래스이다. AWS SNS 클라이언트를 사용하여 메시지를 publish하며, 성공 알림 로직이 활성화된 경우에만 호출된다.

---

## 4. User Interface prototype

본 시스템은 REST API 기반 백엔드 자동화 시스템으로, 별도의 GUI 화면 대신 Swagger UI와 HTTP API 인터페이스를 통해 사용자와 상호작용한다.

### 4.1 Swagger UI

시스템이 실행되면 `/swagger-ui` 에서 Swagger UI를 통해 API 명세를 확인하고 테스트할 수 있다.

```
┌─────────────────────────────────────────────────────────┐
│  KTX-AutoReserve API                                    │
│  Korail KTX 자동 예매 API                                │
├─────────────────────────────────────────────────────────┤
│  GET  /ticket/reserve   티켓 예약 자동화 실행           │
│                                                         │
│  Parameters                                             │
│  ┌──────────────────────────────────────────────────┐   │
│  │ departure   string  required  출발역 (예: 동대구) │   │
│  │ arrival     string  required  도착역 (예: 서울)   │   │
│  │ date        string  required  날짜 (yyyyMMdd)     │   │
│  │ timeStart   integer required  탐색 시작 시간 (0~23)│  │
│  │ timeEnd     integer required  탐색 종료 시간 (0~23)│  │
│  └──────────────────────────────────────────────────┘   │
│                                          [Execute]       │
└─────────────────────────────────────────────────────────┘
```

### 4.2 예약 API 요청

사용자는 아래와 같은 형식으로 HTTP GET 요청을 전송하여 예약을 시도한다.

**Endpoint**: `GET /ticket/reserve`

**Query Parameters**:

| Parameter | Type | Required | Description | Example |
|---|---|---|---|---|
| departure | String | Y | 출발역 | 동대구 |
| arrival | String | Y | 도착역 | 서울 |
| date | String | Y | 출발 날짜 (yyyyMMdd) | 20260501 |
| timeStart | int | Y | 탐색 시작 시간 (0~23) | 6 |
| timeEnd | int | Y | 탐색 종료 시간 (0~23) | 10 |

**요청 예시**:
```
GET /ticket/reserve?departure=동대구&arrival=서울&date=20260501&timeStart=6&timeEnd=10
```

### 4.3 응답 예시

**예약 성공 (200 OK)**:
```json
{
  "message": "예약이 성공적으로 완료되었습니다."
}
```

**파라미터 오류 (400 Bad Request)**:
```json
{
  "message": "필수 파라미터가 누락되었습니다: departure"
}
```

**타입 오류 (400 Bad Request)**:
```json
{
  "message": "파라미터 타입이 올바르지 않습니다: timeStart"
}
```

**탐색 실패**:
```json
{
  "message": "조건에 맞는 예약 가능한 열차를 찾지 못했습니다."
}
```

### 4.4 자동화 실행 흐름

아래는 API 요청 수신 후 시스템 내부에서 실행되는 자동화 흐름이다.

```
[사용자 GET 요청]
       ↓
TicketController
  ├─ 파라미터 검증 실패 → GlobalExceptionHandler → 400 응답
  └─ 파라미터 검증 성공
       ↓
 TicketService
       ↓
 WebDriverFactory → 브라우저 실행
       ↓
 KorailLoginService → 코레일 로그인 (팝업 처리 포함)
       ↓
 화면 이동 → 일반 승차권 예매 페이지
       ↓
 SearchCriteriaService → 출발역 / 도착역 / 날짜 / 시간 입력 및 조회
       ↓
 TicketReservationService → 열차 목록 탐색
  ├─ 조건 불일치 → Polling 후 재조회 ────────────┐
  └─ 조건 일치 → 예약 버튼 클릭                 (반복)
       ├─ 예약 실패 → 재탐색으로 복귀 ────────────┘
       └─ 예약 성공
            ↓
       SmsSender (전화번호 파라미터 존재 시)
            ↓
       WebDriver 세션 종료
            ↓
      [결과 응답 반환]
```

---

## 5. Glossary

| Terms | Description |
|---|---|
| Reservation API | 티켓 예약 자동화 요청 엔드포인트(`/ticket/reserve`) |
| selenium-driverless | CDP(DevTools)로 Chrome 을 제어하는 자동화 라이브러리. Runtime 누수 없이 동작하고 클릭이 isTrusted=true 로 인식되어 매크로 감지를 우회한다. |
| Explicit Wait | 특정 조건이 만족될 때까지 대기하는 동기화 방식 |
| Popup Overlay | 메인 화면 클릭을 가로채는 모달/오버레이 계층 |
| Polling | 일정 간격으로 결과를 재조회하는 반복 전략 |
| KTX Filter | 열차 목록에서 KTX만 선별하는 조건 |
| Time Window | 사용자가 지정한 출발 시각 허용 범위 |
| Exception Handler | 예외를 HTTP 응답으로 변환하는 처리 계층 |
| OpenAPI | REST API 문서화 표준 (Swagger UI 기반) |
| SNS Notification | AWS SNS를 통한 문자 알림 전송 기능 |
| Headless Mode | 화면 출력 없이 브라우저를 백그라운드에서 실행하는 방식 |
| Orchestration | 여러 서비스의 실행 순서를 조율하는 제어 방식 |

---

## 6. References

1. Korail 공식 홈페이지: https://www.korail.com
2. selenium-driverless: https://github.com/kaliiiiiiiiii/Selenium-Driverless
3. FastAPI 공식 문서: https://fastapi.tiangolo.com/
4. AWS SNS 개발자 가이드: https://docs.aws.amazon.com/sns/